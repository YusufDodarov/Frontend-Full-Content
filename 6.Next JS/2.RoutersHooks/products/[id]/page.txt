'use client'
import { useParams, usePathname, useRouter, useSearchParams } from 'next/navigation'
import React, { useEffect } from 'react'

export default  function page({props}) {
    const pathName=usePathname()
    const params=useParams()
    const searchParams=useSearchParams()
    const router=useRouter()
    console.log(pathName)
    console.log(params)
    console.log(searchParams)
    useEffect(()=>{
        console.log('route changed')
    },[])
    return (
    <div>
    <button className='bg-amber-100 m-2' onClick={()=>router.back()}>Back</button>   
    <button className='bg-amber-100 m-2' onClick={()=>router.push('/products')}>Products</button>
    <button className='bg-amber-100 m-2' onClick={()=>router.replace('/products')}>replace</button>
    <button className='bg-amber-100 m-2' onClick={()=>router.refresh()}>refresh</button>
    </div>
  )
}
