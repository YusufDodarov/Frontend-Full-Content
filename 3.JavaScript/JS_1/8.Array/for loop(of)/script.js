// for of / in
let fruits = ["apple", "banana", "orange", "pomegranate", "apricot"];
// in
for (let i in fruits) {
  console.log(`${i}: ${fruits[i]}`);
}
// of
for (let i of fruits) {
  console.log(i);
}
